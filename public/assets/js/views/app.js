// Generated by CoffeeScript 1.7.1
(function() {
  define(['backbone', 'handlebars', 'views/entries', 'views/dashboard', 'views/components/navigation', 'router', 'text!/assets/js/templates/app.hbs'], function(Backbone, Handlebars, EntriesView, DashboardView, NavigationViewComponent, Router, template) {
    return Backbone.View.extend({
      el: 'body',
      initialize: function() {
        this.template = Handlebars.compile(template);
        this.views = {
          dashboard: new DashboardView(),
          entries: new EntriesView()
        };
        this.listenTo(Router, 'route:entries', this.showEntries);
        this.listenTo(Router, 'route:dashboard', this.showDashboard);
        return this.render();
      },
      showEntries: function() {
        return this.content.html(this.views.entries.$el);
      },
      showDashboard: function() {
        return this.content.html(this.views.dashboard.$el);
      },
      render: function() {
        this.$el.html(this.template());
        this.content = this.$el.find('#content');
        return new NavigationViewComponent();
      }
    });
  });

}).call(this);

//# sourceMappingURL=app.map
